import React, { Fragment } from 'react';
import { Book, NavBar } from '../';

const MainPage = () => {
    return (
        <Fragment>
            <NavBar />
            <Book />
        </Fragment>
    );
};

export default MainPage;